// This purpose of this entity is to be used in the Domain Layer and above.
// Feel free to add helpe methods if need be/validation, but no serialization as that is the models
// responsibility.
class RandomCoffeeImageEntity {
  final String file;
  const RandomCoffeeImageEntity({required this.file});
}
